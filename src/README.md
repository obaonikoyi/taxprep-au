# Source structure

- `frontend/` contains the React and TypeScript browser application.
- `backend/TaxPrepAu.Api/` contains the ASP.NET Core Web API.

The applications are separate during development. This keeps the user interface independent from server-side business logic and data access.
