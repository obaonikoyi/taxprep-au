# Captured ATO guidance

Retrieved 18 September 2026 from the URLs in `../source-register.json`. These are readable article-text snapshots, with navigation/scripts removed and the QC identifier retained. SHA-256 hashes apply to the exact UTF-8 files, including their final newline. No source snapshot is silently refreshed at runtime. The build verifies all three hashes and the rule bindings.

© Australian Taxation Office for the Commonwealth of Australia. The source pages permit copying, adaptation, modification, transmission and distribution, provided this does not suggest ATO or Commonwealth endorsement. Neither endorses Xoba Paycheck. Full guidance is retained for traceability; prototype rules cover only the stated subset.

All source applicability mappings, rules and expected examples await qualified tax review. Retrieval is not professional approval. `reviewedAt` and `reviewer` are intentionally null. The 180-day maintenance limit is a conservative project policy, not an ATO requirement or assurance that content remains current. A changed source requires a new snapshot, hash, rule binding, reviewed examples and release. Existing downloaded reports retain the old metadata.
